services:
  oauth2-proxy:
    image: quay.io/oauth2-proxy/oauth2-proxy:latest
    container_name: oauth2-proxy
    restart: always
    ports:
      - "4180:4180"
    environment:
      - OAUTH2_PROXY_PROVIDER=google
      - OAUTH2_PROXY_CLIENT_ID=abc
      - OAUTH2_PROXY_CLIENT_SECRET=def
      - OAUTH2_PROXY_COOKIE_SECRET=5gLsxrHVdq+fbOHqk98FiCOHoLdZYJWk
      - OAUTH2_PROXY_EMAIL_DOMAINS=*
      - OAUTH2_PROXY_REVERSE_PROXY=true
      - OAUTH2_PROXY_HTTP_ADDRESS=0.0.0.0:4180
      - OAUTH2_PROXY_SET_AUTHORIZATION_HEADER=true
      - OAUTH2_PROXY_SET_XAUTHREQUEST=true
      - OAUTH2_PROXY_WHITELIST_DOMAINS=hh.com,cc.com
      - OAUTH2_PROXY_SKIP_PROVIDER_BUTTON=true
      - OAUTH2_PROXY_COOKIE_NAME=_oauth2_proxy
      - OAUTH2_PROXY_COOKIE_SAMESITE=lax
      - OAUTH2_PROXY_SHOW_DEBUG_ON_ERROR=true
      - OAUTH2_PROXY_COOKIE_SECURE=false
      - OAUTH2_PROXY_PASS_ACCESS_TOKEN=true
      - OAUTH2_PROXY_SKIP_AUTH_HEADERS=true
      - OAUTH2_PROXY_BANNER="-"
    command:
      - --http-address=0.0.0.0:4180
      - --upstream=https://hh.com/oauth2/callback
      - --redirect-url=https://hh.com/oauth2/callback
      - --whitelist-domain=hh.com,cc.com
      - --skip-provider-button=true
      - --skip-auth-preflight=true
      - --footer=hh.com
      - --reverse-proxy=true
      - --cookie-secure=true
      - --cookie-samesite=lax
      - --cookie-csrf-per-request=true
      - --cookie-domain=hh.com
      - --cookie-expire=48h0m0s
      - --cookie-csrf-expire=3m
      - --cookie-refresh=1m
      - --relative-redirect-url=true

  nginx-oauth2-proxy:
    image: docker.io/library/nginx:stable-alpine
    container_name: nginx-oauth2-proxy
    restart: always
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
      - ./nginx/hh_com.pem:/etc/nginx/hh_com.pem
      - ./nginx/hh_com.key:/etc/nginx/hh_com.key
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - oauth2-proxy
